I reviewed the current generation code and the newly uploaded `Re851d_v1_1_2_9.docx`. The persistent issue is not just the `eq`/`ne` condition itself: after rendering, checkbox glyph runs are converted into native Word checkbox SDTs, so the existing post-render safety pass that only looks for plain glyph runs can miss them. Also, the uploaded template has extra static owner-occupied checkbox residue in several property blocks (`☐ NO ☐ YES`, `NO / YES`) in addition to the conditional Yes/No lines, which can survive and look like double selection.

Plan:

1. Harden `(eq ...)` / `(ne ...)` conditional evaluation
   - Update the shared parser so `evaluateEqExpression` directly supports both `eq` and `ne` instead of converting `ne` indirectly.
   - Update the s-expression evaluator so nested `(ne ...)` expressions inside `and`, `or`, or `not` work consistently.
   - Keep existing `eq` behavior unchanged.

2. Make the RE851D owner-occupied safety pass SDT-aware
   - In `generate-document/index.ts`, update the post-render RE851D owner-occupied pass to recognize both:
     - plain glyph runs: `☐`, `☑`, `☒`
     - native Word checkbox SDTs generated by `convertGlyphsToSdtCheckboxes`
   - Add helpers to rewrite an existing SDT checkbox state and display glyph safely:
     - checked: `w14:checked w14:val="1"`, displayed `☑`
     - unchecked: `w14:checked w14:val="0"`, displayed `☐`
   - This is the main missing piece because the current final pass runs after glyph-to-SDT conversion but only searches for glyph runs.

3. Force exactly one pair in every OWNER OCCUPIED cell/block
   - For each property section #1-#5, locate the `OWNER OCCUPIED` anchor.
   - Within the bounded owner-occupied area only, identify the real Yes and No labels and their preceding checkbox controls.
   - Rewrite those controls based only on `pr_p_occupanc_K`:
     - `Owner Occupied` -> Yes checked, No unchecked
     - anything else, including Tenant / Other, Vacant, NA, blank -> Yes unchecked, No checked

4. Remove extra owner-occupied residue that causes duplicate visible checkbox pairs
   - Within only the owner-occupied bounded area, remove leftover static fragments such as:
     - `☐ NO ☐ YES`
     - standalone `NO` / `YES` lines that are not the canonical `Yes` / `No` labels
     - leftover conditional tokens if any survived rendering
   - Do not touch tax delinquency Yes/No, lien questions, or any other checkbox sections.

5. Preserve current scope
   - No UI changes.
   - No database/schema changes.
   - No template upload flow changes.
   - No unrelated document generation changes.

Validation after implementation:

- Use the uploaded template structure as the target pattern.
- Verify property index mapping for `pr_p_occupanc_1` through `pr_p_occupanc_5`.
- Confirm the final generated DOCX XML cannot contain both owner-occupied Yes and No checked in the same property block, whether the controls are glyph runs or native Word SDTs.